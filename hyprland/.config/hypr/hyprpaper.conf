# IPC on → wallpaper can be changed live via `hyprctl hyprpaper ...`
# (used by ~/.config/hypr/wallpaper.sh)
ipc = on
splash = false

preload = ~/.config/backgrounds/wallpaper
wallpaper = ,~/.config/backgrounds/wallpaper
